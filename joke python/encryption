import random

def rand(seed):
    seed =
string = ""
def encrypt(string):
    string = input("Enter string: ")
    for i in string:
        i = random.random()
        rand = random.random()
        print((i**1/2)**3)
        print(round(i, (rand)))

print(rand)

encrypt(string)