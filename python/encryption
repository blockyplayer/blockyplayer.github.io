import random

def rand(seed):
    seed = random.randint(-999999999, 999999999)
string = ""
def encrypt(string):
    string = input("Enter string: ")
    for i in string:
        i = random.random()
        rand = random.random()
        print((i**1/2)**3)
        print(round(i, (rand)))

print(rand)

encrypt(string)