from file_compression import 

'''
Method where the key is in the encrypted string. The index of each digit of the unlock key is known, as the index
is the key that is transmitted. So there's a key to find the key, then the message can be decrypted. 
'''

inp = input('String: ')

